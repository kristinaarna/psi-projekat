'use strict';

const LanguageChoice = require('./lang-choice');
const CanvasContainer = require('./canvas-container');
const Configuration = require('./configuration');

module.exports = {
  LanguageChoice,
  CanvasContainer,
  Configuration,
};